// src/utils/utils.js
export const formatTranslationResult = (result) => {
  // Format the translation result if necessary (for example, structure or clean up data).
  return result.translations; // Just a placeholder logic, update as needed
};
